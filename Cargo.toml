[package]
name = "webified-template"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = {version = "1.50.0", features = ["full"]}
axum = "0.8.8"
tower-http = {version = "0.6.8", features = ["full"]}
http = "1.4.0"
